@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>
@if (User.Identity.IsAuthenticated)
{
    @Html.ActionLink("Show logs", "Index", "Logger")
}
else
{
    @Html.ActionLink("Login", "Index", "Logger")
}
